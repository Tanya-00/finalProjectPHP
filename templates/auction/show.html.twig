{% extends 'base.html.twig' %}

{% block title %}Auction{% endblock %}

{% block body %}
    <h1>Auction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ auction.id }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ auction.address }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ auction.date }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('auction_index') }}">back to list</a>

    <a href="{{ path('auction_edit', {'id': auction.id}) }}">edit</a>

    {{ include('auction/_delete_form.html.twig') }}
{% endblock %}
