<form method="post" action="{{ path('byuer_delete', {'id': byuer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ byuer.id) }}">
    <button class="btn">Delete</button>
</form>
