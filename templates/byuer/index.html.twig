{% extends 'base.html.twig' %}

{% block title %}Byuer index{% endblock %}

{% block body %}
    <h1>Byuer index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Phone</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for byuer in byuers %}
            <tr>
                <td>{{ byuer.id }}</td>
                <td>{{ byuer.name }}</td>
                <td>{{ byuer.phone }}</td>
                <td>
                    <a href="{{ path('byuer_show', {'id': byuer.id}) }}">show</a>
                    <a href="{{ path('byuer_edit', {'id': byuer.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('byuer_new') }}">Create new</a>
{% endblock %}
