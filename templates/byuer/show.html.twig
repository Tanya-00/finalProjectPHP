{% extends 'base.html.twig' %}

{% block title %}Byuer{% endblock %}

{% block body %}
    <h1>Byuer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ byuer.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ byuer.name }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ byuer.phone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('byuer_index') }}">back to list</a>

    <a href="{{ path('byuer_edit', {'id': byuer.id}) }}">edit</a>

    {{ include('byuer/_delete_form.html.twig') }}
{% endblock %}
